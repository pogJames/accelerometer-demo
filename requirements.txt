# Runtime — required
Flask==3.1.3
numpy==2.4.4
pandas==3.0.2
pyserial==3.5

# Runtime — optional
# TFLite: this build imports tflite_runtime (ships with NXP eIQ, not on PyPI).
# For the Matrix800/VeriSilicon port, use ai-edge-litert instead (see README).
pyfftw